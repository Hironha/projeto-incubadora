import type { WSMessage } from '@interfaces/utility/connection';

export interface IConnectionEventOutput extends WSMessage<boolean> {}

export interface IConnectionEventOutput extends WSMessage<boolean> {}
